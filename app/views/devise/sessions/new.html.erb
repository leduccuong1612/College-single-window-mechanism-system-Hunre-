
<section class="section">
	<div class="container">
		<div class="columns is-centered">
			  <div class="column is-4 box has-background-success">
				<h2 class="title is-2 has-text-white">Đăng Nhập</h2>
				<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

				  <div class="field">
				  	<div class="control">
				    	<%= f.input :login_name, label: "Mã Tài Khoản", required: false, input_html: { class: "input"}, wrapper: false, label_html: { class: "label  has-text-white" } %>
				    </div>
				  </div>

				  <div class="field">
				  	<div class="control">
				      <%= f.input :password, label: "Mật Khẩu", required: false, input_html: { class: "input "}, wrapper: false, label_html: { class: "label  has-text-white" } %>
						</div>
					</div>

					<div class="field">
				  	<div class="control">
				    <%= f.input :remember_me, label: "Ghi nhớ tài khoản của tôi", wrapper: false, label_html: {class: "has-text-white"}, as: :boolean if devise_mapping.rememberable? %>
				  	</div>
					</div>

				  <%= f.button :submit, "Đăng Nhập", class:"button is-warning is-medium" %>
				<% end %>
				<br/>
				<%= render "devise/shared/links" %>
      </div>
		</div>
	</div>
</section>
